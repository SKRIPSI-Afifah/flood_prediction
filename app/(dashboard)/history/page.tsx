"use client";

import {
  FileText,
  Table,
  TrendingUp,
  Filter,
  MoreVertical,
  Eye,
  ArrowRight,
  Activity,
  AlertTriangle,
  ShieldCheck,
  Users,
} from "lucide-react";
import { Button } from "@/components/ui/button";
import { useState, useEffect } from "react";

const MOCK_RECORDS = [
  {
    date: "Oct 24, 2023",
    time: "14:22:10 WIB",
    params: { rain: "45mm", tide: "2.1m", soil: "88%" },
    status: "Critical Flood Risk",
    statusVariant: "error",
    authority: "SA",
    authorityName: "SysAdmin",
  },
  {
    date: "Oct 23, 2023",
    time: "09:15:45 WIB",
    params: { rain: "12mm", tide: "1.4m", soil: "42%" },
    status: "Low Risk",
    statusVariant: "secondary",
    authority: "BK",
    authorityName: "B. Kurniawan",
  },
  {
    date: "Oct 22, 2023",
    time: "21:05:30 WIB",
    params: { rain: "28mm", tide: "1.8m", soil: "75%" },
    status: "Moderate Risk",
    statusVariant: "tertiary",
    authority: "SA",
    authorityName: "SysAdmin",
  },
  {
    date: "Oct 22, 2023",
    time: "10:44:12 WIB",
    params: { rain: "5mm", tide: "1.1m", soil: "20%" },
    status: "Safe State",
    statusVariant: "secondary",
    authority: "MR",
    authorityName: "M. Ridwan",
  },
];

export default function HistoryPage() {
  const [records, setRecords] = useState<any[]>([]);
  const [loading, setLoading] = useState(true);

  useEffect(() => {
    const fetchHistory = async () => {
      try {
        const res = await fetch('/api/history');
        const data = await res.json();
        setRecords(data);
      } catch (error) {
        console.error("Failed to fetch history", error);
      } finally {
        setLoading(false);
      }
    };
    fetchHistory();
  }, []);

  return (
    <div className="space-y-12 animate-in fade-in duration-700 pb-12">
      {/* Header */}
      <div className="flex flex-col md:flex-row md:items-end justify-between gap-8 mb-12">
        <div>
          <h2 className="text-4xl font-black text-primary tracking-tighter uppercase italic mb-3">Prediction Archive</h2>
          <p className="text-muted-foreground max-w-xl font-medium">
            Comprehensive ledger of all flood modeling simulations and risk assessments generated by the GIS Sentinel engine.
          </p>
        </div>
        <div className="flex gap-4">
          <Button variant="outline" className="h-14 px-8 rounded-2xl border-primary/20 text-primary font-black uppercase text-[10px] tracking-widest hover:bg-primary/5">
            <FileText className="w-4 h-4 mr-3" /> Export PDF
          </Button>
          <Button className="h-14 px-10 rounded-2xl bg-primary text-white font-black uppercase text-[10px] tracking-widest shadow-xl shadow-primary/20">
            <Table className="w-4 h-4 mr-3" /> Export Excel
          </Button>
        </div>
      </div>

      {/* Stats Bar */}
      <div className="grid grid-cols-1 md:grid-cols-4 gap-6">
        {[
          { label: "Total Simulations", value: "1,284", icon: Activity, trend: "+12%", color: "text-primary" },
          { label: "Critical Warnings", value: "42", icon: AlertTriangle, trend: "Last 30 days", color: "text-tertiary" },
          { label: "Accuracy Mean", value: "94.8%", icon: ShieldCheck, trend: "Stable", color: "text-secondary" },
          { label: "Active Users", value: "18", icon: Users, trend: "Operators", color: "text-primary" },
        ].map((item, idx) => (
          <div key={idx} className="bg-card p-8 rounded-[3rem] border border-border/10 shadow-sm group hover:shadow-lg transition-all">
            <p className="text-[10px] font-black uppercase tracking-[0.2em] text-muted-foreground mb-4">{item.label}</p>
            <div className="flex items-end justify-between">
              <span className={`text-3xl font-black tracking-tighter ${item.color}`}>{item.value}</span>
              <div className="flex flex-col items-end">
                <item.icon className={`w-5 h-5 ${item.color} mb-1 opacity-40`} />
                <span className="text-[9px] font-black uppercase tracking-widest text-muted-foreground/60">{item.trend}</span>
              </div>
            </div>
          </div>
        ))}
      </div>

      {/* Table Section */}
      <div className="bg-card rounded-[3rem] overflow-hidden shadow-2xl border border-border/10">
        <div className="px-10 py-6 bg-muted/60 border-b border-border/10 flex justify-between items-center">
          <h3 className="text-sm font-black text-primary uppercase tracking-widest italic">Historical Log</h3>
          <div className="flex gap-3">
            <Button variant="ghost" size="icon" className="w-10 h-10 rounded-xl hover:bg-white/40">
              <Filter className="w-5 h-5 text-primary" />
            </Button>
            <Button variant="ghost" size="icon" className="w-10 h-10 rounded-xl hover:bg-white/40">
              <MoreVertical className="w-5 h-5 text-primary" />
            </Button>
          </div>
        </div>

        <div className="overflow-x-auto">
          <table className="w-full text-left border-collapse">
            <thead>
              <tr className="bg-muted/30 border-b border-border/5">
                <th className="px-10 py-6 text-[10px] font-black uppercase tracking-widest text-muted-foreground">Date / Time</th>
                <th className="px-10 py-6 text-[10px] font-black uppercase tracking-widest text-muted-foreground">Input Parameters</th>
                <th className="px-10 py-6 text-[10px] font-black uppercase tracking-widest text-muted-foreground">Result / Status</th>
                <th className="px-10 py-6 text-[10px] font-black uppercase tracking-widest text-muted-foreground">Authority</th>
                <th className="px-10 py-6 text-[10px] font-black uppercase tracking-widest text-muted-foreground">Actions</th>
              </tr>
            </thead>
            <tbody className="divide-y divide-border/5">
              {records.map((record, idx) => (
                <tr key={idx} className="hover:bg-muted/20 transition-colors group">
                  <td className="px-10 py-6">
                    <div className="flex flex-col gap-1">
                      <span className="text-sm font-black text-primary italic lowercase tracking-tight">{record.date}</span>
                      <span className="text-[10px] font-bold text-muted-foreground uppercase">{record.time}</span>
                    </div>
                  </td>
                  <td className="px-10 py-6">
                    <div className="flex flex-wrap gap-2">
                      {Object.entries(record.params).map(([k, v]) => (
                        <span key={k} className="px-3 py-1 bg-primary/5 rounded-full text-[9px] font-black text-primary border border-primary/5 uppercase">
                          {k}: {String(v)}
                        </span>
                      ))}
                    </div>
                  </td>
                  <td className="px-10 py-6">
                    <div className="flex items-center gap-3">
                      <div className={`w-2.5 h-2.5 rounded-full ${record.statusVariant === 'error' ? 'bg-destructive' :
                          record.statusVariant === 'secondary' ? 'bg-secondary' : 'bg-tertiary'
                        } animate-pulse shadow-sm`}></div>
                      <span className={`text-xs font-black uppercase tracking-widest italic animate-in fade-in duration-1000 ${record.statusVariant === 'error' ? 'text-destructive' :
                          record.statusVariant === 'secondary' ? 'text-secondary' : 'text-tertiary'
                        }`}>{record.status}</span>
                    </div>
                  </td>
                  <td className="px-10 py-6">
                    <div className="flex items-center gap-3">
                      <div className="w-8 h-8 rounded-xl bg-primary/10 flex items-center justify-center text-[10px] font-black text-primary border border-primary/10">
                        {record.authority}
                      </div>
                      <span className="text-xs font-bold text-muted-foreground whitespace-nowrap">{record.authorityName}</span>
                    </div>
                  </td>
                  <td className="px-10 py-6">
                    <Button variant="ghost" size="icon" className="w-10 h-10 rounded-xl text-primary opacity-40 group-hover:opacity-100 transition-opacity hover:bg-white/40">
                      <Eye className="w-5 h-5" />
                    </Button>
                  </td>
                </tr>
              ))}
            </tbody>
          </table>
        </div>

        <div className="px-10 py-6 bg-muted/30 border-t border-border/10 flex items-center justify-between">
          <p className="text-[10px] font-black text-muted-foreground uppercase tracking-widest">Showing 4 of 1,284 records</p>
          <div className="flex gap-2">
            {[1, 2, 3].map((p) => (
              <Button key={p} variant={p === 1 ? 'default' : 'outline'} className={`w-10 h-10 rounded-xl text-[10px] font-black ${p === 1 ? 'shadow-lg shadow-primary/20' : 'border-border/10 text-muted-foreground'}`}>
                {p}
              </Button>
            ))}
          </div>
        </div>
      </div>

      {/* Contextual Grid */}
      <div className="mt-12 grid grid-cols-1 lg:grid-cols-3 gap-8 pb-12">
        <div className="lg:col-span-2 relative h-72 rounded-[4rem] overflow-hidden shadow-2xl group border-[8px] border-white">
          <div className="absolute inset-0 bg-cover bg-center transition-transform duration-1000 group-hover:scale-110" style={{ backgroundImage: "url('https://lh3.googleusercontent.com/aida-public/AB6AXuA6CNQ-XCaOZIovYDqqTp4NZClA-6rCxOGaJ4O27iGx1BOxMfQBVljtnuBDi3HqjEMjAYobW3nZ41fF_pidLRhtdgSZfsrH1a-xA1TnGGH1n6KDNTuWf4VypxiXtESnGtnhyj8yfhLnEgBT-Rrsje-yqgCzCjQ74-diJVJKxoOFdF84V6WRi2JzJu9SMZ0n69p-_JWWq0T9OhsjxhFGo5MvV-gD5amBJHVQj-s7hqTNnERHQtYib74mmszkLpZ8G60dBrCopIDEuN8')" }}></div>
          <div className="absolute inset-0 bg-gradient-to-t from-primary/95 via-primary/30 to-transparent"></div>
          <div className="absolute bottom-10 left-10 right-10">
            <h4 className="text-3xl font-black text-white uppercase italic tracking-tighter mb-2">Spatial Context View</h4>
            <p className="text-white/70 text-sm max-w-xl font-medium leading-relaxed">All archived predictions are geographically anchored to the GIS sentinel grid. View historical trends on the interactive map for pattern recognition.</p>
          </div>
        </div>
        <div className="bg-primary text-white p-12 rounded-[4rem] shadow-2xl flex flex-col justify-between group hover:scale-[1.02] transition-all">
          <div>
            <div className="w-16 h-16 bg-white/10 rounded-[2rem] flex items-center justify-center mb-8 border border-white/10 group-hover:rotate-12 transition-transform">
              <TrendingUp className="w-8 h-8 text-secondary" />
            </div>
            <h4 className="text-2xl font-black uppercase italic tracking-tight mb-4 leading-none">Trend Analysis</h4>
            <p className="text-white/60 text-sm font-medium leading-relaxed">Generated reports now include automated shift detection. Our algorithm identifies if new predictions deviate significantly from average.</p>
          </div>
          <Button variant="ghost" className="mt-10 px-0 flex justify-start items-center gap-3 text-secondary font-black uppercase tracking-widest text-[10px] hover:bg-transparent hover:text-white transition-colors">
            Explore Modeling Suite <ArrowRight className="w-4 h-4" />
          </Button>
        </div>
      </div>
    </div>
  );
}
